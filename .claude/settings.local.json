{
  "permissions": {
    "allow": [
      "mcp__Claude_Preview__preview_start",
      "Bash(npm install:*)",
      "Bash(gh pr create --title 'feat: Add dev server launch config and lock file' --body ':*)"
    ]
  }
}
